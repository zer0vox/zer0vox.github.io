{
  "name": "pzcel-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies pzcel Ask AI requests to OpenAI, keeping the API key server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
